{{ printf "<?xml version= \"1.0\" encoding= \"utf-8\" standalone= \"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
    xmlns:xhtml="http://www.w3.org/1999/xhtml">
    {{ range .Data.Pages }}
    <url>{{ with .Permalink }}
        <loc>{{ . }}</loc>
        {{- end -}}
        {{ with .Date }}
        <date>{{ time.Format "2006-01-02" . }}</date>
        {{ end }}
        {{- with .Title -}}
        <title>{{ htmlUnescape . }}</title>
        {{- end -}}
        {{ with .Summary }}
        <description>{{ . | transform.Plainify | transform.XMLEscape }}</description>
        {{- end -}}
        {{ with .Params.tags }}
        <tags>{{ delimit . "," }}</tags>
        {{- end -}}
        {{ with .Params.classes }}
        <class>{{ delimit . "," }}</class>
        {{- end -}}
        {{ with .Sitemap.ChangeFreq }}
        <changefreq>{{ . }}</changefreq>
        {{- end -}}
        {{ if ge .Sitemap.Priority 0.0 }}
        <priority>{{ .Sitemap.Priority }}</priority>
        {{- end -}}
        {{ if .IsTranslated }}
            {{ range .Translations }}
        <xhtml:link
            rel="alternate"
            hreflang="{{ .Language.Lang }}"
            href="{{ .Permalink }}"
        />
            {{- end -}}
        <xhtml:link
            rel="alternate"
            hreflang="{{ .Language.Lang }}"
            href="{{ .Permalink }}"
        />
        {{- end -}}
    </url>
    {{- end -}}
</urlset>